import 'package:flutter/widgets.dart';

class LayoutDesktop extends StatelessWidget {
  const LayoutDesktop({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
